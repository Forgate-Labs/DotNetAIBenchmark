# Inventory reservation status codes

Fix POST /inventory/{sku}/reservations so stock reservations are validated and return correct status codes.

Only public tests are available in this workspace. Hidden tests will be used by the benchmark after your changes.

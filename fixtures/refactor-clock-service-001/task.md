# Clock abstraction refactoring

Refactor TrialService to use an injectable clock so expiration logic is deterministic and testable.

Only public tests are available in this workspace. Hidden tests will be used by the benchmark after your changes.

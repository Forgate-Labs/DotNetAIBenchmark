# Owner or admin authorization

Fix DELETE /notes/{id} so note owners and admins can delete notes, while other users cannot. The fixture uses X-User and X-Role headers.

Only public tests are available in this workspace. Hidden tests will be used by the benchmark after your changes.

# Product creation validation

Fix POST /products so invalid product payloads are rejected and valid products are created.

Only public tests are available in this workspace. Hidden tests will be used by the benchmark after your changes.

# EF Core tenant filtering

Fix InvoiceRepository.ListForTenantAsync so it never returns invoices from another tenant.

Only public tests are available in this workspace. Hidden tests will be used by the benchmark after your changes.

# Tenant authorization

Fix document access so a caller can only read documents from its own tenant. The tenant is supplied by the X-Tenant header for this fixture.

Only public tests are available in this workspace. Hidden tests will be used by the benchmark after your changes.

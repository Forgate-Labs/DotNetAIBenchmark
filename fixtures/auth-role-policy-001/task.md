# Role authorization

Fix the admin report endpoint role check. The fixture uses X-Role to keep authentication small and deterministic.

Only public tests are available in this workspace. Hidden tests will be used by the benchmark after your changes.

# Order total calculation

Fix the order total returned by POST /orders/quote. The total must include each line quantity and reject invalid input.

Only public tests are available in this workspace. Hidden tests will be used by the benchmark after your changes.

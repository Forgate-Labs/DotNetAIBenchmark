# Profile JSON contract

Fix GET /profiles/{id} to return the documented JSON contract without leaking internal field names.

Only public tests are available in this workspace. Hidden tests will be used by the benchmark after your changes.

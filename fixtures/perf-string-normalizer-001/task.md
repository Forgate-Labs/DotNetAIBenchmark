# Slug generator allocations

Optimize SlugGenerator.Generate so it remains correct while avoiding excessive allocations for large input.

Only public tests are available in this workspace. Hidden tests will be used by the benchmark after your changes.
